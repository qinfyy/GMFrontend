# 2026-09-13

## GM 协议大重构适配（GMFrontend）

上游 `D:\Il2Cpp\bh2\Sv` 把 GM 接口从「结构化查询参数」重构为**命令行风格**：
- 请求：`GET /api/gm?content=<整条命令行>`，如 `/give weapon 1001 x2 lv80 r5 @10001`
- 响应：`{success, messages[]}`（不再是 before/after 结构化对象）
- 修饰符：`x数量 lv r s p i t pt bl ml` + `-flag` + `@uid`

### 改动
- 新增 `src/app/core/command-line.ts`（cmdLine/arg/mod/xAmount/flag）
- 重写 `core/gm-api.service.ts`（content 传参 + messages 归一化 + help 文本解析 + 3 版外壳容错）
- `shared/` 三件套改造；8 个页面全部改为拼命令行；console 页重做为自由命令行 + 命令速查
- `core/handbook.service.ts` 新增 `parseGmTemplate`/`normalizeGmTemplate`
- 同步 `public/Handbook.txt`（8679 行）；CLAUDE.md / STATE.md / DEVLOG.md / FEATURES.md 全部更新

### 验证
对真实服务器（18:57 构建）实测：成功外壳 37 条 messages、`/help` 解析出 12 条命令（label/别名/用法/notes 全对）、4 条错误路径状态码与中文提示正确。

### 踩坑
1. Angular CLI 22 拒绝受管 Node 22.22.2，需 `export PATH="/c/Program Files/nodejs:$PATH"` 用系统 Node 24。
2. `/help` 列表模式会把多行打包进一条 message（`\r\n`），解析前必须摊平。
3. 上游同日两次改响应外壳（18:29 带 retcode → 18:54 仅 success+errorDescription+messages），客户端做了多版本容错。
4. 本机有 `http_proxy=http://127.0.0.1:60017`：服务端没起来时 curl 会拿到代理的错误页（"upstream connect failed"），就绪判断必须用 `curl -f` 或日志标记 `Application started`，否则误判。
5. 启动 Sv.exe 约 25 秒（加载 88 张资源表 + 重新生成 Handbook.txt），且后台进程会随工具回合结束被回收，验证要写在同一条命令里。

## 跟进（19:55）

上游又动了两次，均无需改解析逻辑：
- `OtherCommands.ExecuteHelp` 去掉用法/notes 缩进（`      /usage` → `/usage`、`  注:` → `注:`），且不再把多行打包进单条 message
- `GameMasterService` 把 `GmCommandResJson.Success` 改名 `Ok`；注册表仍 12 条
- 解析器统一用 `^\s*` + 先按换行摊平，两版格式都能吃

UI 调整（用户要求）：`command-bar` 预览去掉 `GET` 前缀，「复制 URL」改为「复制命令」（复制命令行本身），删掉随之失效的 `GmApiService.urlFor`。

验证：对用户自己起的后端（localhost:21000）实测 12 条命令解析全对；`yarn build` 通过。

## 措辞约定（用户指定）

结果面板成功徽标文案为 **「发送成功」**（不是「成功」）——因为服务器即使业务失败也可能返回 success。改动文件：`shared/result-panel.ts`。

## 后续清理与提交（20:35）

- `gm-api.service.ts` 按要求**只认当前版响应外壳** `{success, errorDescription, messages}`，移除 `result` 包裹 / `retcode` / `error` / `error_description` 全部兼容分支；`GmApiError` 构造从 4 参简化为 3 参（去掉从未被读取的 `description`），同步 2 处调用点。
- 提交两次：`713d677 feat(gm): 适配上游命令行式 GM 协议`、`979b2bc chore(handbook): 同步上游 Handbook（文件头新增生成时间行）`。
- **注意**：`public/Handbook.txt` 曾在 20:22 被外部删除（非本会话操作），已恢复。提交前若 `git status` 出现 `D public/Handbook.txt`，直接从 `Sv/Handbook.txt` 重新 copy。
- 校验方法：`grep "^\[" Handbook.txt` 与 `help.page.ts` 的 `KNOWN_SECTIONS` 逐字比对；裸标题分区（`命令`/`类型说明`/`剧情关卡目录`）是已知无害项，不在 KNOWN_SECTIONS 里也不会显示。
